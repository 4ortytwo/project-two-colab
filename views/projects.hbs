{{#each projects}}
<div>
    <h1><a href="/projects/{{id}}">{{projectName}}</a></h1>
    <p>{{description}}</p>
    <h2>CREATED BY: <a href='/auth/profile/{{createdBy.id}}'>{{createdBy.firstName}}</a></h2>
    <h3>Due {{deadlineFormatted}}</h3>
</div>
<br><br>
{{/each}}